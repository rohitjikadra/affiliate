# AffiliateHub frontend

Next.js app for the affiliate product discovery platform.

```bash
copy .env.example .env.local
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
