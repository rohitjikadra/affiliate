API_ORIGIN=http://localhost:4000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_CONTACT_EMAIL=

# Same value as backend REVALIDATE_SECRET. Lets the price worker refresh ISR pages after a successful fetch.
REVALIDATE_SECRET=
